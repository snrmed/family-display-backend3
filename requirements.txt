# --- Core framework ---
flask==3.0.3
gunicorn==22.0.0

# --- Image generation and processing ---
pillow==10.4.0
requests==2.32.3

# --- Google Cloud integration ---
google-cloud-storage==2.17.0
google-auth==2.33.0

# --- Optional but recommended (for safety & CORS if needed later) ---
flask-cors==4.0.1

# --- Hugging Face / Pexels / External calls (used by backend) ---
huggingface-hub==0.25.2
pexels-api==1.0.1

# --- Other utilities ---
python-dotenv==1.0.1
